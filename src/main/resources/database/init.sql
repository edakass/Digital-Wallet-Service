-- Create the database (will fail if it already exists)
CREATE DATABASE digital_wallet;

-- Alternatively, create the database only if it does not exist
CREATE DATABASE IF NOT EXISTS digital_wallet;

-- Select the database for use
USE digital_wallet;